@using E_Ticaret_Sitesi.Entity;
@model IEnumerable<Category>
@{
    var selectedCategory = Url.RequestContext.RouteData.Values["id"]; //eğer id varsa atar yoksa null olur
}

    <div class="list-group">
        <a href="/Home/List" class="list-group-item list-group-item-action">
            <i class="fas fa-angle-double-right"></i> Tüm Kategoriler
        </a>
        @foreach (var item in Model)
        {
            if (selectedCategory != null)
            {
                if (item.Id == int.Parse(selectedCategory.ToString()))
                {
                    //seçilen kategori ile route id'si birbirine eşit
                    <a href="/Home/List/@item.Id" class="list-group-item list-group-item-action active">
                        <i class="fas fa-angle-double-right"></i> @item.Name
                    </a>
                }
                else
                {
                    <a href="/Home/List/@item.Id" class="list-group-item list-group-item-action">
                        <i class="fas fa-angle-double-right"></i> @item.Name
                    </a>
                }
            }
            else
            {
                <a href="/Home/List/@item.Id" class="list-group-item list-group-item-action">
                    <i class="fas fa-angle-double-right"></i> @item.Name
                </a>
            }
        }
    </div>